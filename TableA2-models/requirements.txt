# TableA2-models / acs_apr_models.py — Python dependencies
pandas>=2.0
numpy>=1.24
requests>=2.28
matplotlib>=3.7
scipy>=1.10
pymc>=5.0
statsmodels>=0.14
arch>=6.0
firthmodels>=0.3
tqdm>=4.65
scikit-learn>=1.3

# -----------------------------------------------------------------------------
# Local inputs and caches (not installed by pip). Paths are relative to this
# folder unless noted. These supplement Census / FRED / NHGIS / Zillow APIs —
# anything listed as “optional” is skipped or auto-downloaded when missing.
# -----------------------------------------------------------------------------
#
# --- Required local CSV (pipeline raises if absent) ---
#   tablea2.csv
#
# --- Zillow Research CSVs (optional; charts/joins use NaNs if missing) ---
#   City_zhvi_uc_sfrcondo_tier_0.33_0.67_sm_sa_month.csv
#   City_zori_uc_sfrcondomfr_sm_sa_month.csv
#   Zip_zhvi_uc_sfrcondo_tier_0.33_0.67_sm_sa_month.csv
#   Zip_zori_uc_sfrcondomfr_sm_sa_month.csv
#
# --- Census / crosswalk files (optional; downloaded and saved here if absent) ---
#   place_county_relationship.csv   (Census place–county; pipe-delimited source)
#   county_cbsa_relationship.csv      (NBER CBSA–county crosswalk)
#   national_county2020.txt         (Census county codes; used for CA county FIPS)
#
# --- API-backed caches (JSON; created or refreshed at runtime, not hand-edited) ---
#   nhgis_cache.json
#   nhgis_cache_2018_place_b19013_b01003.json
#   cpi_cache.json                  (FRED CPI-U; set FRED_API_KEY or allow prompt)
#   geocode_cache.json              (Census batch geocoder results)
#   acs_zcta_income_cache.json     (Census API ZCTA income cache)
#
# --- Generated outputs (not inputs) ---
#   r2_diagnostics.csv
#   *.png                           (charts written next to the script)
